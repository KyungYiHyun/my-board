server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/my-board?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 0000
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    root: info

mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mappers/**/*.xml
  type-aliases-package: com.my_board