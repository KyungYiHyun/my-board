<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.my_board.member.mapper.MemberMapper">
    <insert id="insert" parameterType="member.entity.Member">
        INSERT INTO member(login_id, member_name, password, birth, nickname)
        VALUES (#{loginId}, #{memberName},#{password},#{birth},#{nickname})
    </insert>

</mapper>