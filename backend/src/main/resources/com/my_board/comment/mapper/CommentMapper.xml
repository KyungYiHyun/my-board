<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.my_board.comment.mapper.CommentMapper">

    <insert id="createComment" useGeneratedKeys="true" keyProperty="comment.id">
        INSERT INTO comments(member_id, post_id, content)
        VALUES (#{comment.memberId}, #{postId}, #{comment.content})
    </insert>

    <select id="getComments" parameterType="Long" resultType="GetCommentsResponse">
        SELECT c.id AS commentId, m.nickname, c.content, c.created_at, c.modified_at
        FROM comments c
        JOIN member m ON c.member_id = m.id
        WHERE c.post_id = #{postId}
    </select>

    <update id="updateComment">
        UPDATE comments
        SET content = #{comment.content}
        WHERE id = #{commentId}
    </update>
</mapper>